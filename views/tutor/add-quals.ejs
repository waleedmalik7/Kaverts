<!DOCTYPE html>
<html lang = "en">
    <head>
        <title>Kaverts</title>
        <meta name="viewport" content="width=device-width">
        <meta charset="utf-8">
        <link href="../tutor-css/add-quals.css" rel="stylesheet">
        <link rel="icon" href="../images/Logo.png">
    </head>

    <body>
        <header>
            <a href = "/"> <button id="home-button" > Home </button></a>
            <a href="/signout"> <button id="sign-out" > Sign Out </button></a>
            <a href="/profile"><button id="profile-button" > Profile </button></a>
        </header>
        <div class="post-container">
            
            <form id="qual-form" method = "POST" action = "/add-quals">
                <div class="title-qual">
                    <h2 class="header">Add qualification for a subject</h2>
                </div><select class = 'selectInput' name="subject">
                    <option disabled selected value="">Subject</option>
                    <% for (var i = 0; i < subjects.length; i++) { %>
                        <option value="<%= subjects[i].name %>"><%= subjects[i].name %></option>
                    <% } %>
                </select>
                <select class = 'selectInput' name="grade">
                    <option disabled selected value="">Grade</option>
                    <% for (var i = 0; i < academicLevels.length; i++) { %>
                        <option value="<%= academicLevels[i].level %>"><%= academicLevels[i].name %></option>
                    <% } %>
                </select>
                <input name="proof" type="text" class="textInput" placeholder="Some Text">
                <input class="submit" type="submit" value="SUBMIT">
                <label> &emsp; <%-prompt%> </label>
            </form>
        </div>
        <footer>
            <p class="copyright">Copyright 2023 Kaverts &copy;</p>
        </footer>
    </body>
    
</html>